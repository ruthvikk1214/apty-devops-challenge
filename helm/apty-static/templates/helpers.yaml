cat <<'EOF' > _helpers.tpl
{{/*
Expand the name of the chart.
*/}}
{{- define "apty-static.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" }}
{{- end }}

{{/*
Create a default fully qualified app name.
*/}}
{{- define "apty-static.fullname" -}}
{{- if .Values.fullnameOverride }}
{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- $name := default .Chart.Name .Values.nameOverride }}
{{- if contains $name .Release.Name }}
{{- .Release.Name | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- printf "%s-%s" .Release.Name $name | trunc 63 | trimSuffix "-" }}
{{- end }}
{{- end }}
{{- end }}

{{/*
Common labels
*/}}
{{- define "apty-static.labels" -}}
helm.sh/chart: {{ include "apty-static.name" . }}-{{ .Chart.Version | replace "+" "_" }}
{{ include "apty-static.selectorLabels" . }}
{{- if .Chart.AppVersion }}
app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
{{- end }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- end }}

{{/*
Selector labels
*/}}
{{- define "apty-static.selectorLabels" -}}
app.kubernetes.io/name: {{ include "apty-static.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
EOF